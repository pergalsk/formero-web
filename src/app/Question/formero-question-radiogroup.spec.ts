import { FormeroQuestionRadiogroup } from './formero-question-radiogroup';

describe('FormeroRadiogroup', () => {
  it('should create an instance', () => {
    expect(new FormeroQuestionRadiogroup({})).toBeTruthy();
  });
});
