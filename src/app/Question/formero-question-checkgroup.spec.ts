import { FormeroQuestionCheckgroup } from './formero-question-checkgroup';

describe('FormeroQuestionCheckgroup', () => {
  it('should create an instance', () => {
    expect(new FormeroQuestionCheckgroup({})).toBeTruthy();
  });
});
