import { FormeroQuestionDropdown } from './formero-question-dropdown';

describe('FormeroQuestionDropdown', () => {
  it('should create an instance', () => {
    expect(new FormeroQuestionDropdown({})).toBeTruthy();
  });
});
