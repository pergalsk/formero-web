import { FormeroQuestionTextbox } from './formero-question-textbox';

describe('FormeroQuestionTextbox', () => {
  it('should create an instance', () => {
    expect(new FormeroQuestionTextbox({})).toBeTruthy();
  });
});
