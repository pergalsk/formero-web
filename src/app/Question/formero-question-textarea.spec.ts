import { FormeroQuestionTextarea } from './formero-question-textarea';

describe('FormeroQuestionTextarea', () => {
  it('should create an instance', () => {
    expect(new FormeroQuestionTextarea()).toBeTruthy();
  });
});
