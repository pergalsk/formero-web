import { FormeroBlockTitle } from './formero-block-title';

describe('FormeroBlockTitle', () => {
  it('should create an instance', () => {
    expect(new FormeroBlockTitle({})).toBeTruthy();
  });
});
